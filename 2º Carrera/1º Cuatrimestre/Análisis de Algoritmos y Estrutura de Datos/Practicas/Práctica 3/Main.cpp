#include<iostream>
#include "polinomio.h"
using namespace std;

int main(){
    unsigned gradoMax=5;
    polinomio p1(gradoMax);
    polinomio p2(gradoMax);
    polinomio paux(0);
    p1.coeficiente(0,1);
    
    switch (opt)
    {
    case 1:
        break;
    
    default:
        break;
    }

    return 0;
}